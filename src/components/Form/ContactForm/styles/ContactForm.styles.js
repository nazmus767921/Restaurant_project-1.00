import styled from "styled-components";

export const FormWrapper = styled.div`
	/* elements inside */
	.form-control {
		display: flex;
		flex-direction: column;
		gap: 2.5em;
	}
	.submit--btn {
		margin: 0 auto;
	}
`;
